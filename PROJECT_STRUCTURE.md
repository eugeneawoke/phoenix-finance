# Phoenix Finance Revolution — Полная структура проекта

> **Текущий стек (актуально):** Next.js + **Sanity** (CMS). Личный кабинет, дашборд, Payload CMS, БД (PostgreSQL/Drizzle), миграции и Docker **не используются** и в проекте отключены.

## Содержание

1. [Обзор архитектуры](#1-обзор-архитектуры)
2. [Технологический стек](#2-технологический-стек)
3. [Дизайн-система](#3-дизайн-система)
4. [Frontend — структура и компоненты](#4-frontend)
5. [Backend — API и бизнес-логика](#5-backend)
6. [База данных — схема и модели](#6-база-данных)
7. [CMS — управление контентом](#7-cms)
8. [Платежи и эквайринг](#8-платежи-и-эквайринг)
9. [AI-ассистент и боты](#9-ai-ассистент-и-боты)
10. [Интеграции (Salesforce, WhatsApp, соцсети)](#10-интеграции)
11. [Безопасность](#11-безопасность)
12. [Развёртывание (DevOps)](#12-развёртывание)
13. [Итерации разработки](#13-итерации-разработки)

---

## 1. Обзор архитектуры

```
┌─────────────────────────────────────────────────────────────────┐
│                        КЛИЕНТЫ                                  │
│  Desktop Browser │ Mobile Browser │ WhatsApp │ Telegram         │
└────────┬────────────────┬──────────────┬──────────┬─────────────┘
         │                │              │          │
         ▼                ▼              ▼          ▼
┌─────────────────────────────────────────────────────────────────┐
│                     CDN / Edge (Vercel / Cloudflare)            │
│                 Static assets, ISR кэш, GeoIP                  │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌─────────────────────────────────────────────────────────────────┐
│                    NEXT.JS 15 (App Router)                      │
│                                                                 │
│  ┌──────────┐  ┌──────────┐  ┌──────────┐                       │
│  │ Лендинг  │  │ Услуги   │  │  Блог/   │                       │
│  │ (SSG)    │  │ (SSR)    │  │  Обучение │                       │
│  │          │  │          │  │  (ISR)    │                       │
│  └──────────┘  └──────────┘  └──────────┘                       │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │              Sanity CMS (Studio, контент)                 │   │
│  │  Контент через Sanity Studio │ next-sanity │ i18n         │   │
│  └──────────────────────────────────────────────────────────┘   │
│                                                                 │
│  ┌──────────────────────────────────────────────────────────┐   │
│  │                    API Routes (Next.js)                    │   │
│  │  /api/payments │ /api/crm │ /api/ai │ /api/webhooks      │   │
│  └──────────────────────────────────────────────────────────┘   │
└────────────────────────────┬────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────┐
│  (БД, Redis, Docker не используются — только Sanity + Vercel) │
└──────────────────────────────────────────────────────────────┘
              │
              ├──────────────────────────────────┐
              ▼                                  ▼
┌──────────────────────┐          ┌──────────────────────┐
│   Salesforce CRM     │          │  Внешние сервисы     │
│   (REST API)         │          │  - Bank of Georgia   │
│   - Воронки          │          │  - Crypto acquiring  │
│   - Контакты         │          │  - OpenAI API        │
│   - Партнёры         │          │  - WhatsApp API      │
│   - Сделки           │          │  - SendGrid/Resend   │
└──────────────────────┘          └──────────────────────┘
```

### Ключевой принцип: Next.js + Sanity CMS

**Текущий стек:**
- **Next.js** — фронт, API routes, SSR/SSG/ISR
- **Sanity** — headless CMS: контент редактируется в Sanity Studio (`/studio`), доставляется через next-sanity
- Личный кабинет, Payload, БД (PostgreSQL/Drizzle), Docker и миграции в проекте не используются

---

## 2. Технологический стек

### Frontend

| Технология | Назначение | Почему |
|---|---|---|
| **Next.js 15** (App Router) | Фреймворк | SSR/SSG/ISR, API routes, отличная SEO, поддержка i18n |
| **TypeScript 5.x** | Язык | Строгая типизация, меньше ошибок |
| **Tailwind CSS 4** | Стилизация | Быстрая разработка, кастомная тема, responsive |
| **Framer Motion** | Анимации | Claymorphism-эффекты, плавные переходы |
| **next-intl** | i18n | EN/GE/RU с определением по GeoIP |
| **Zustand** | State management | Лёгкий, простой, для клиентского состояния |
| **React Hook Form + Zod** | Формы | Валидация, производительность |
| **Lucide Icons** | Иконки | Консистентный набор SVG-иконок |
| **Recharts** | Графики | Для визуализации данных на сайте (при необходимости) |

### Backend

| Технология | Назначение | Почему |
|---|---|---|
| **Sanity** | CMS | Headless CMS, контент в Sanity Studio, next-sanity на фронте |
| **Node.js 20 LTS** | Runtime | Стабильная версия |
| *(БД, Redis, Docker не используются в текущем проекте)* |

### Сервисы и интеграции

| Сервис | Назначение |
|---|---|
| **Salesforce** (REST API) | CRM — воронки, контакты, сделки, партнёры |
| **Bank of Georgia** (API) | Приём платежей в GEL, USD, EUR |
| **Crypto acquiring** (TBD) | Крипто-платежи |
| **OpenAI API** (GPT-4o) | AI-ассистент с RAG |
| **WhatsApp Business API** | Обработка заявок, чат с клиентами |
| **Resend / SendGrid** | Транзакционные email |
| **Vercel / Cloudflare** | Хостинг, CDN, Edge Functions |

### DevOps

| Технология | Назначение |
|---|---|
| **Vercel** | Деплой (рекомендуется для Next.js + Sanity) |
| **GitHub Actions** | CI/CD (по необходимости) |
| **Sentry** | Мониторинг ошибок (опционально) |
| *(Docker в проекте не используется)* |

---

## 3. Дизайн-система

### Цветовая палитра (на основе логотипа Phoenix)

```
ОСНОВНАЯ ТЕМА (Phoenix Dark + Gold)

Первичные цвета:
  --phoenix-navy:      #0A0E27    ← Глубокий тёмно-синий (основной фон)
  --phoenix-navy-800:  #141938    ← Вторичный фон
  --phoenix-navy-700:  #1E2548    ← Карточки, элементы

Золотые акценты:
  --phoenix-gold:      #D4A517    ← Основной золотой (CTA, акценты)
  --phoenix-gold-light:#F0C94D    ← Светлый золотой (hover)
  --phoenix-gold-dark: #B8920F    ← Тёмный золотой (pressed)

Дополнительные:
  --phoenix-white:     #FAFAF9    ← Текст на тёмном фоне
  --phoenix-gray:      #9CA3AF    ← Вторичный текст
  --phoenix-gray-dark: #4B5563    ← Placeholder, мелкий текст

Семантические:
  --success:           #10B981    ← Успех, подтверждение
  --warning:           #F59E0B    ← Предупреждение
  --error:             #EF4444    ← Ошибка
  --info:              #3B82F6    ← Информация

Секция НКО (альтернативный акцент):
  --ngo-purple:        #7C3AED    ← Фиолетовый для НКО-раздела
  --ngo-purple-light:  #A78BFA

Светлая тема (для отдельных страниц/секций):
  --bg-light:          #FAFAF9
  --bg-light-card:     #FFFFFF
  --text-dark:         #0C0A09
  --text-muted:        #44403C
```

### Типографика

```
Заголовки:  Playfair Display (400, 500, 600, 700)
            — элегантный, премиальный, подходит для финансового сектора
            — поддерживает латиницу (EN, GE при транслитерации)

Основной:   Inter (300, 400, 500, 600, 700)
            — отличная читаемость, нейтральный, поддержка кириллицы и грузинского

Грузинский: Noto Sans Georgian (400, 500, 600, 700)
            — лучшая поддержка грузинского алфавита

CSS Import:
@import url('https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;500;600;700&family=Inter:wght@300;400;500;600;700&family=Noto+Sans+Georgian:wght@400;500;600;700&display=swap');

Tailwind Config:
fontFamily: {
  serif: ['Playfair Display', 'serif'],
  sans: ['Inter', 'Noto Sans Georgian', 'sans-serif'],
}

Размеры (мобильный / десктоп):
  H1:  32px / 56px  (line-height: 1.1)
  H2:  28px / 42px  (line-height: 1.2)
  H3:  22px / 28px  (line-height: 1.3)
  Body: 16px / 18px  (line-height: 1.6)
  Small: 14px / 14px (line-height: 1.5)
```

### Стиль: Premium Dark + Claymorphism элементы

```css
/* Claymorphism карточка */
.clay-card {
  background: linear-gradient(145deg, #1E2548, #141938);
  border-radius: 24px;
  box-shadow:
    8px 8px 16px rgba(0, 0, 0, 0.4),
    -4px -4px 12px rgba(255, 255, 255, 0.05),
    inset 0 1px 0 rgba(255, 255, 255, 0.08);
  border: 1px solid rgba(255, 255, 255, 0.06);
  backdrop-filter: blur(10px);
}

/* Золотой CTA */
.cta-gold {
  background: linear-gradient(135deg, #D4A517 0%, #F0C94D 50%, #D4A517 100%);
  color: #0A0E27;
  font-weight: 600;
  border-radius: 12px;
  box-shadow:
    0 4px 14px rgba(212, 165, 23, 0.35),
    inset 0 1px 0 rgba(255, 255, 255, 0.3);
  transition: all 300ms cubic-bezier(0.4, 0, 0.2, 1);
}

.cta-gold:hover {
  box-shadow:
    0 6px 20px rgba(212, 165, 23, 0.5),
    inset 0 1px 0 rgba(255, 255, 255, 0.4);
  transform: translateY(-1px);
}

/* Glassmorphism навбар */
.navbar-glass {
  background: rgba(10, 14, 39, 0.85);
  backdrop-filter: blur(20px) saturate(180%);
  border-bottom: 1px solid rgba(255, 255, 255, 0.08);
}

/* Крупная кнопка "Подписаться на новости" (как на референсе) */
.subscribe-banner {
  background: #0A0E27;
  border-radius: 20px;
  padding: 48px 64px;
  display: flex;
  justify-content: space-between;
  align-items: center;
  /* иконка самолётика с анимацией */
}
```

### Анимации

```
Микро-взаимодействия:  150–300ms (ease-out)
Переходы страниц:     300–500ms (cubic-bezier)
Появление элементов:  stagger fade-in + translateY(20px)
Числа/счётчики:       countUp animation
Hover на карточках:    translateY(-2px) + shadow increase
Кнопки:               scale(1.02) + glow effect

Уважение prefers-reduced-motion:
@media (prefers-reduced-motion: reduce) {
  *, *::before, *::after {
    animation-duration: 0.01ms !important;
    transition-duration: 0.01ms !important;
  }
}
```

---

## 4. Frontend

### Структура файлов

```
phoenix-finance/
├── src/
│   ├── app/                              # Next.js App Router
│   │   ├── (frontend)/                   # Группа маршрутов — публичный сайт
│   │   │   ├── [locale]/                 # i18n: /en, /ge, /ru
│   │   │   │   ├── layout.tsx            # Основной layout с navbar + footer
│   │   │   │   ├── page.tsx              # Главная страница / лендинг
│   │   │   │   │
│   │   │   │   ├── services/             # Бухгалтерские услуги
│   │   │   │   │   ├── page.tsx          # Перечень всех услуг
│   │   │   │   │   └── [slug]/
│   │   │   │   │       └── page.tsx      # Детальная страница услуги
│   │   │   │   │
│   │   │   │   ├── ngo/                  # НКО-раздел
│   │   │   │   │   ├── page.tsx          # Главная НКО
│   │   │   │   │   ├── events/           # События
│   │   │   │   │   │   ├── page.tsx
│   │   │   │   │   │   └── [slug]/page.tsx
│   │   │   │   │   ├── partners/         # Партнёры НКО (публичный список)
│   │   │   │   │   │   └── page.tsx
│   │   │   │   │   └── donate/           # Донаты
│   │   │   │   │       └── page.tsx
│   │   │   │   │
│   │   │   │   ├── education/            # Обучение / Блог
│   │   │   │   │   ├── page.tsx          # Главная обучения
│   │   │   │   │   ├── blog/             # Статьи
│   │   │   │   │   │   ├── page.tsx
│   │   │   │   │   │   └── [slug]/page.tsx
│   │   │   │   │   └── videos/           # Видео-контент
│   │   │   │   │       ├── page.tsx
│   │   │   │   │       └── [slug]/page.tsx
│   │   │   │   │
│   │   │   │   ├── partnership/          # Партнёрская программа
│   │   │   │   │   ├── page.tsx          # Страница "Стать партнёром"
│   │   │   │   │   ├── sponsors/page.tsx # Для спонсоров
│   │   │   │   │   ├── interns/page.tsx  # Для стажёров
│   │   │   │   │   └── stipendiants/     # Для стипендиатов (мечта)
│   │   │   │   │       └── page.tsx
│   │   │   │   │
│   │   │   │   ├── about/               # О компании
│   │   │   │   │   └── page.tsx
│   │   │   │   │
│   │   │   │   └── contact/             # Обратная связь
│   │   │   │       └── page.tsx
│   │   │   │
│   │   │   └── middleware.ts             # GeoIP → определение языка
│   │   │
│   │   # (dashboard не используется — личный кабинет отключён)
│   │   │
│   │   ├── (auth)/                       # Группа — авторизация (при необходимости)
│   │   │   ├── login/page.tsx
│   │   │   ├── register/page.tsx
│   │   │   ├── forgot-password/page.tsx
│   │   │   └── reset-password/page.tsx
│   │   │
│   │   # (Payload не используется — CMS: Sanity Studio)
│   │   │
│   │   ├── api/                          # API Routes
│   │   │   ├── payments/
│   │   │   │   ├── bog/route.ts          # Bank of Georgia webhook
│   │   │   │   ├── crypto/route.ts       # Crypto webhook
│   │   │   │   └── create/route.ts       # Создание платежа
│   │   │   ├── crm/
│   │   │   │   ├── sync/route.ts         # Salesforce sync
│   │   │   │   └── webhook/route.ts      # Salesforce webhook
│   │   │   ├── ai/
│   │   │   │   ├── chat/route.ts         # AI-ассистент (streaming)
│   │   │   │   └── train/route.ts        # Обучение/обновление базы
│   │   │   ├── contact/
│   │   │   │   └── route.ts              # Форма обратной связи
│   │   │   ├── newsletter/
│   │   │   │   └── route.ts              # Подписка на новости
│   │   │   ├── whatsapp/
│   │   │   │   └── webhook/route.ts      # WhatsApp webhook
│   │   │   └── partnership/
│   │   │       └── apply/route.ts        # Заявка на партнёрство
│   │   │
│   │   ├── layout.tsx                    # Root layout
│   │   └── globals.css                   # Глобальные стили + Tailwind
│   │
│   ├── components/
│   │   ├── ui/                           # Базовые UI-компоненты
│   │   │   ├── Button.tsx
│   │   │   ├── Card.tsx                  # Clay-card
│   │   │   ├── Input.tsx
│   │   │   ├── Modal.tsx
│   │   │   ├── Badge.tsx
│   │   │   ├── Tabs.tsx
│   │   │   ├── Accordion.tsx
│   │   │   ├── Toast.tsx
│   │   │   ├── Skeleton.tsx
│   │   │   ├── LanguageSwitcher.tsx
│   │   │   └── AdBanner.tsx              # Место для рекламы (образоват.)
│   │   │
│   │   ├── layout/
│   │   │   ├── Navbar.tsx                # Glassmorphism навбар
│   │   │   ├── Footer.tsx                # Футер с реквизитами
│   │   │   └── MobileMenu.tsx
│   │   │
│   │   ├── landing/                      # Секции лендинга
│   │   │   ├── Hero.tsx                  # Hero с Phoenix-анимацией
│   │   │   ├── ServicesOverview.tsx       # Обзор услуг
│   │   │   ├── WhyUs.tsx                 # Почему мы
│   │   │   ├── Stats.tsx                 # Цифры/достижения
│   │   │   ├── Testimonials.tsx          # Отзывы
│   │   │   ├── NGOBanner.tsx             # Блок про НКО
│   │   │   ├── EducationBanner.tsx       # Блок про обучение
│   │   │   ├── SubscribeBanner.tsx       # "Подписаться на новости"
│   │   │   ├── ContactForm.tsx           # Форма обратной связи
│   │   │   ├── CompanyDetails.tsx        # Реквизиты компании
│   │   │   └── SocialLinks.tsx           # Ссылки на соцсети
│   │   │
│   │   ├── services/
│   │   │   ├── ServiceCard.tsx           # Карточка услуги
│   │   │   ├── ServiceList.tsx           # Список услуг
│   │   │   ├── PricingSection.tsx        # Цены (если применимо)
│   │   │   └── OrderService.tsx          # Заказать услугу + оплата
│   │   │
│   │   ├── education/
│   │   │   ├── ArticleCard.tsx           # Карточка статьи (как на jara.ge)
│   │   │   ├── RelatedArticles.tsx       # "Related articles" секция
│   │   │   ├── VideoCard.tsx             # Карточка видео
│   │   │   └── CategoryFilter.tsx        # Фильтр по категориям
│   │   │
│   │   ├── ngo/
│   │   │   ├── EventCard.tsx
│   │   │   ├── DonateWidget.tsx          # Виджет донатов
│   │   │   └── PartnerCard.tsx
│   │   │
│   │   # dashboard-компоненты не используются (личный кабинет отключён)
│   │   │
│   │   ├── chat/
│   │   │   ├── AIChatWidget.tsx          # Плавающий виджет AI
│   │   │   ├── ChatMessage.tsx
│   │   │   └── ChatInput.tsx
│   │   │
│   │   └── shared/
│   │       ├── SEO.tsx                   # Meta tags
│   │       ├── Breadcrumbs.tsx
│   │       ├── Pagination.tsx
│   │       ├── SearchBar.tsx
│   │       └── ErrorBoundary.tsx
│   │
│   ├── lib/
│   │   # (payload не используется — CMS: Sanity, см. src/sanity/)
│   │   │
│   │   ├── integrations/
│   │   │   ├── salesforce/
│   │   │   │   ├── client.ts             # Salesforce REST client
│   │   │   │   ├── sync.ts              # Синхронизация данных
│   │   │   │   └── types.ts
│   │   │   ├── payments/
│   │   │   │   ├── bog.ts                # Bank of Georgia SDK
│   │   │   │   ├── crypto.ts             # Crypto acquiring
│   │   │   │   ├── types.ts
│   │   │   │   └── provider.ts           # Абстракция платёжного провайдера
│   │   │   ├── whatsapp/
│   │   │   │   ├── client.ts             # WhatsApp Business API
│   │   │   │   └── templates.ts          # Шаблоны сообщений
│   │   │   ├── ai/
│   │   │   │   ├── assistant.ts          # OpenAI + RAG
│   │   │   │   ├── knowledge-base.ts     # Векторная база знаний
│   │   │   │   ├── embeddings.ts         # Генерация embeddings
│   │   │   │   └── prompts.ts            # Системные промпты
│   │   │   └── email/
│   │   │       ├── client.ts             # Resend/SendGrid
│   │   │       └── templates/            # Email-шаблоны (HTML)
│   │   │
│   │   ├── i18n/
│   │   │   ├── config.ts                 # Настройка next-intl
│   │   │   ├── request.ts                # Определение локали
│   │   │   └── messages/
│   │   │       ├── en.json               # Английские переводы
│   │   │       ├── ge.json               # Грузинские переводы
│   │   │       └── ru.json               # Русские переводы
│   │   │
│   │   ├── utils/
│   │   │   ├── cn.ts                     # classnames utility
│   │   │   ├── format.ts                 # Форматирование дат, валют
│   │   │   ├── geo.ts                    # GeoIP определение региона
│   │   │   └── security.ts              # Sanitization, encryption helpers
│   │   │
│   │   └── hooks/                        # React hooks
│   │       ├── useAuth.ts
│   │       ├── useLocale.ts
│   │       └── useMediaQuery.ts
│   │
│   ├── styles/
│   │   ├── theme.ts                      # CSS переменные, темы
│   │   └── clay.css                      # Claymorphism утилиты
│   │
│   └── types/
│       ├── index.ts                      # Общие типы
│       ├── services.ts
│       ├── orders.ts
│       └── api.ts
│
├── public/
│   ├── images/
│   │   ├── phoenix-logo.svg              # Логотип (векторный)
│   │   ├── phoenix-logo-gold.svg         # Логотип золотой вариант
│   │   └── og-image.jpg                  # Open Graph
│   ├── fonts/                            # Локальные шрифты (если нужно)
│   └── locales/                          # Статические переводы
│
├── scripts/                              # Вспомогательные скрипты
│   ├── seed.ts                           # Начальные данные (при необходимости)
│   └── sync-salesforce.ts                # Скрипт синхронизации (при необходимости)
│
│   # migrations/ и docker/ не используются
│
├── .env.example                          # Переменные окружения (шаблон)
├── .env.local                            # Локальные переменные (не в git!)
├── next.config.ts
├── tailwind.config.ts
├── tsconfig.json
├── package.json
└── README.md
```

### Структура страниц (Sitemap)

```
КОММЕРЧЕСКАЯ ЧАСТЬ (Commercial)
/                           → Главная (лендинг)
/services                   → Все бухгалтерские услуги
/services/[slug]            → Детальная страница услуги
/about                      → О компании
/contact                    → Обратная связь

НКО (Non-Profit)
/ngo                        → Главная НКО
/ngo/events                 → События
/ngo/events/[slug]          → Детали события + донаты
/ngo/partners               → Партнёры НКО (публичный список)
/ngo/donate                 → Общий донат

ОБУЧЕНИЕ / БЛОГ (Education)
/education                  → Главная обучения
/education/blog             → Блог (статьи)
/education/blog/[slug]      → Статья
/education/videos           → Видео-контент
/education/videos/[slug]    → Видео

ПАРТНЁРСКАЯ ПРОГРАММА
/partnership                → Стать партнёром (обзор)
/partnership/sponsors       → Для спонсоров
/partnership/interns        → Для стажёров
/partnership/stipendiants   → Для стипендиатов

# Личный кабинет (dashboard) не используется

АВТОРИЗАЦИЯ (при необходимости)
/login                      → Вход
/register                   → Регистрация
/forgot-password            → Восстановление пароля

CMS
/studio                     → Sanity Studio (редактирование контента)
```

### Секции главной страницы (лендинг)

```
┌─────────────────────────────────────────────────────────┐
│  NAVBAR (glassmorphism, fixed)                           │
│  Logo │ Услуги │ НКО │ Обучение │ О нас │ EN/GE/RU     │
├─────────────────────────────────────────────────────────┤
│                                                         │
│  HERO SECTION                                           │
│  ┌─────────────────────────────────────────────────┐    │
│  │  Заголовок: "Ваш надёжный финансовый партнёр"   │    │
│  │  Подзаголовок: краткое описание                  │    │
│  │  [Получить консультацию]  [Наши услуги]         │    │
│  │              Phoenix-логотип с анимацией          │    │
│  └─────────────────────────────────────────────────┘    │
│                                                         │
│  SERVICES OVERVIEW (карточки с claymorphism)            │
│  ┌────────┐ ┌────────┐ ┌────────┐ ┌────────┐          │
│  │Бухучёт │ │Налоги  │ │Аудит   │ │Консал- │          │
│  │        │ │        │ │        │ │тинг    │          │
│  └────────┘ └────────┘ └────────┘ └────────┘          │
│  [Все услуги →]                                         │
│                                                         │
│  WHY US / STATS                                         │
│  ┌─────┐  ┌─────┐  ┌─────┐  ┌─────┐                   │
│  │ 500+│  │ 15+ │  │ 3   │  │ 98% │                   │
│  │клиен│  │ лет  │  │языка│  │довол│                   │
│  └─────┘  └─────┘  └─────┘  └─────┘                   │
│                                                         │
│  РЕКВИЗИТЫ КОМПАНИИ                                     │
│  Адрес │ Телефон │ ИНН │ Банк                          │
│                                                         │
│  НКО BANNER                                             │
│  ┌─────────────────────────────────────────────────┐    │
│  │  "Образование и развитие"                       │    │
│  │  Кратко о некоммерческой части                   │    │
│  │  [Узнать больше →]                              │    │
│  └─────────────────────────────────────────────────┘    │
│                                                         │
│  EDUCATION BANNER                                       │
│  ┌─────────────────────────────────────────────────┐    │
│  │  Последние статьи и материалы                   │    │
│  │  [Card] [Card] [Card]  (related articles стиль) │    │
│  │  [Смотреть все →]                               │    │
│  └─────────────────────────────────────────────────┘    │
│                                                         │
│  РЕКЛАМНЫЙ БАННЕР (образовательный контент)            │
│  ┌─────────────────────────────────────────────────┐    │
│  │  Место для контекстной рекламы (образовательной) │    │
│  └─────────────────────────────────────────────────┘    │
│                                                         │
│  SUBSCRIBE BANNER (как на референсе — крупная кнопка)  │
│  ┌─────────────────────────────────────────────────┐    │
│  │  "Оставайтесь в курсе новостей"                 │    │
│  │  Подпишитесь на наш Telegram-канал              │    │
│  │  [📨 Подписаться]          ✈️ (иконка)          │    │
│  └─────────────────────────────────────────────────┘    │
│                                                         │
│  CONTACT FORM                                           │
│  Имя │ Email │ Телефон │ Сообщение │ [Отправить]       │
│  (без капчи, с honeypot + rate limiting)               │
│                                                         │
│  FOOTER                                                 │
│  ┌─────────────────────────────────────────────────┐    │
│  │  Логотип │ Навигация │ Соцсети │ Реквизиты     │    │
│  │  Коммерч. соцсети │ НКО соцсети                 │    │
│  │  © 2025 Phoenix Finance Revolution              │    │
│  └─────────────────────────────────────────────────┘    │
│                                                         │
│  AI CHAT WIDGET (плавающий, правый нижний угол)        │
│  ┌──────┐                                               │
│  │ 💬   │ ← раскрывается в чат                         │
│  └──────┘                                               │
└─────────────────────────────────────────────────────────┘
```

---

## 5. Backend

### Backend и данные

Контент управляется через **Sanity** (схемы в `src/sanity/schemaTypes/`). Payload CMS, БД (PostgreSQL/Drizzle) и миграции в проекте не используются.

*(Ниже — справочное описание моделей для возможных API/интеграций; не привязано к Payload.)*

#### Пример структуры пользователя (справочно)

```typescript
// Не используется в текущем проекте (нет БД/Payload)
{
  slug: 'users',
  auth: {
    tokenExpiration: 7200,      // 2 часа
    maxLoginAttempts: 5,        // блокировка после 5 попыток
    lockTime: 600 * 1000,       // блокировка на 10 минут
  },
  fields: [
    { name: 'firstName', type: 'text', required: true, localized: true },
    { name: 'lastName', type: 'text', required: true, localized: true },
    { name: 'phone', type: 'text' },
    { name: 'company', type: 'text' },
    { name: 'role', type: 'select',
      options: ['client', 'partner', 'manager', 'admin'],
      defaultValue: 'client'
    },
    { name: 'preferredLanguage', type: 'select',
      options: ['en', 'ge', 'ru'],
      defaultValue: 'en'
    },
    { name: 'salesforceId', type: 'text', admin: { readOnly: true } },
    { name: 'section', type: 'select',
      options: ['commercial', 'ngo', 'both'],
      defaultValue: 'commercial'
    },
  ],
  access: {
    read: isAdminOrSelf,
    update: isAdminOrSelf,
    delete: isAdmin,
  }
}
```

#### Services (Бухгалтерские услуги)

```typescript
{
  slug: 'services',
  admin: { useAsTitle: 'title' },
  fields: [
    { name: 'title', type: 'text', required: true, localized: true },
    { name: 'slug', type: 'text', unique: true },
    { name: 'description', type: 'richText', localized: true },
    { name: 'shortDescription', type: 'textarea', localized: true },
    { name: 'icon', type: 'text' }, // Lucide icon name
    { name: 'category', type: 'select',
      options: [
        'accounting',    // Бухгалтерский учёт
        'tax',           // Налоги
        'audit',         // Аудит
        'consulting',    // Консалтинг
        'registration',  // Регистрация бизнеса
        'payroll',       // Зарплатный учёт
        'reporting',     // Отчётность
        'international', // Международный учёт
      ]
    },
    { name: 'pricing', type: 'group', fields: [
      { name: 'type', type: 'select',
        options: ['fixed', 'hourly', 'monthly', 'custom'] },
      { name: 'amount', type: 'number' },
      { name: 'currency', type: 'select',
        options: ['GEL', 'USD', 'EUR'] },
      { name: 'description', type: 'text', localized: true },
    ]},
    { name: 'features', type: 'array', fields: [
      { name: 'feature', type: 'text', localized: true }
    ]},
    { name: 'published', type: 'checkbox', defaultValue: true },
    { name: 'order', type: 'number', defaultValue: 0 },
    { name: 'image', type: 'upload', relationTo: 'media' },
  ],
  // Маркетолог может редактировать через Sanity Studio (/studio)
  access: {
    read: () => true,
    create: isAdminOrManager,
    update: isAdminOrManager,
    delete: isAdmin,
  }
}
```

#### Orders (Заказы)

```typescript
{
  slug: 'orders',
  fields: [
    { name: 'orderNumber', type: 'text', unique: true },
    { name: 'client', type: 'relationship', relationTo: 'users', required: true },
    { name: 'service', type: 'relationship', relationTo: 'services' },
    { name: 'status', type: 'select',
      options: ['pending', 'confirmed', 'in_progress', 'review',
                'completed', 'cancelled'],
      defaultValue: 'pending'
    },
    { name: 'amount', type: 'number' },
    { name: 'currency', type: 'select', options: ['GEL', 'USD', 'EUR'] },
    { name: 'paymentStatus', type: 'select',
      options: ['unpaid', 'partial', 'paid', 'refunded'] },
    { name: 'paymentMethod', type: 'select',
      options: ['bank_of_georgia', 'crypto', 'bank_transfer'] },
    { name: 'notes', type: 'richText' },
    { name: 'documents', type: 'relationship',
      relationTo: 'documents', hasMany: true },
    { name: 'salesforceOpportunityId', type: 'text' },
  ],
  hooks: {
    afterChange: [syncOrderToSalesforce, sendOrderNotification],
  }
}
```

#### Invoices (Счета)

```typescript
{
  slug: 'invoices',
  fields: [
    { name: 'invoiceNumber', type: 'text', unique: true },
    { name: 'order', type: 'relationship', relationTo: 'orders' },
    { name: 'client', type: 'relationship', relationTo: 'users' },
    { name: 'amount', type: 'number', required: true },
    { name: 'currency', type: 'select', options: ['GEL', 'USD', 'EUR'] },
    { name: 'status', type: 'select',
      options: ['draft', 'sent', 'paid', 'overdue', 'cancelled'] },
    { name: 'dueDate', type: 'date' },
    { name: 'paidDate', type: 'date' },
    { name: 'items', type: 'array', fields: [
      { name: 'description', type: 'text', localized: true },
      { name: 'quantity', type: 'number' },
      { name: 'unitPrice', type: 'number' },
      { name: 'total', type: 'number' },
    ]},
    { name: 'pdf', type: 'upload', relationTo: 'media' },
  ]
}
```

#### Documents (Документы клиентов)

```typescript
{
  slug: 'documents',
  fields: [
    { name: 'title', type: 'text', required: true },
    { name: 'file', type: 'upload', relationTo: 'media' },
    { name: 'owner', type: 'relationship', relationTo: 'users', required: true },
    { name: 'order', type: 'relationship', relationTo: 'orders' },
    { name: 'type', type: 'select',
      options: ['contract', 'report', 'tax_return', 'invoice',
                'receipt', 'certificate', 'other'] },
    { name: 'confidential', type: 'checkbox', defaultValue: true },
    { name: 'encryptionKey', type: 'text',
      admin: { hidden: true } // Ключ шифрования (не показывать)
    },
  ],
  access: {
    read: isOwnerOrAdmin,    // Только владелец или админ
    create: isAuthenticated,
    update: isAdmin,
    delete: isAdmin,
  },
  hooks: {
    beforeChange: [encryptDocumentFile],   // Шифрование при загрузке
    afterRead: [decryptDocumentFile],       // Дешифровка при чтении
  }
}
```

#### BlogPosts (Статьи)

```typescript
{
  slug: 'blog-posts',
  admin: { useAsTitle: 'title' },
  fields: [
    { name: 'title', type: 'text', required: true, localized: true },
    { name: 'slug', type: 'text', unique: true },
    { name: 'content', type: 'richText', localized: true },
    { name: 'excerpt', type: 'textarea', localized: true },
    { name: 'featuredImage', type: 'upload', relationTo: 'media' },
    { name: 'category', type: 'select',
      options: ['accounting', 'tax', 'legal', 'business', 'ngo', 'education'] },
    { name: 'tags', type: 'array', fields: [
      { name: 'tag', type: 'text' }
    ]},
    { name: 'author', type: 'relationship', relationTo: 'users' },
    { name: 'section', type: 'select',
      options: ['commercial', 'ngo', 'education'],
      required: true
    },
    { name: 'publishedDate', type: 'date' },
    { name: 'status', type: 'select',
      options: ['draft', 'published', 'archived'] },
    { name: 'viewCount', type: 'number', defaultValue: 0 },
    { name: 'relatedPosts', type: 'relationship',
      relationTo: 'blog-posts', hasMany: true },
  ],
  access: {
    read: ({ doc }) => doc?.status === 'published' || isAdmin,
    create: isAdminOrManager,
    update: isAdminOrManager,
  }
}
```

#### Events (События НКО)

```typescript
{
  slug: 'events',
  fields: [
    { name: 'title', type: 'text', required: true, localized: true },
    { name: 'slug', type: 'text', unique: true },
    { name: 'description', type: 'richText', localized: true },
    { name: 'date', type: 'date', required: true },
    { name: 'endDate', type: 'date' },
    { name: 'location', type: 'text', localized: true },
    { name: 'isOnline', type: 'checkbox' },
    { name: 'meetingLink', type: 'text' },
    { name: 'image', type: 'upload', relationTo: 'media' },
    { name: 'donationGoal', type: 'number' },
    { name: 'donationRaised', type: 'number', defaultValue: 0 },
    { name: 'status', type: 'select',
      options: ['upcoming', 'live', 'completed', 'cancelled'] },
    { name: 'registeredUsers', type: 'relationship',
      relationTo: 'users', hasMany: true },
  ]
}
```

#### Partners (Партнёры НКО)

```typescript
{
  slug: 'partners',
  fields: [
    { name: 'name', type: 'text', required: true, localized: true },
    { name: 'logo', type: 'upload', relationTo: 'media' },
    { name: 'website', type: 'text' },
    { name: 'description', type: 'textarea', localized: true },
    { name: 'type', type: 'select',
      options: ['sponsor', 'partner', 'media_partner'] },
    { name: 'section', type: 'select',
      options: ['commercial', 'ngo'], // НКО — публичный, Commercial — скрытый
      required: true
    },
    { name: 'isPublic', type: 'checkbox', defaultValue: false },
    { name: 'order', type: 'number', defaultValue: 0 },
  ],
  access: {
    read: ({ doc }) => doc?.isPublic || isAdmin,
  }
}
```

#### PartnerApplications (Заявки в партнёрскую программу)

```typescript
{
  slug: 'partner-applications',
  fields: [
    { name: 'applicantName', type: 'text', required: true },
    { name: 'company', type: 'text' },
    { name: 'email', type: 'email', required: true },
    { name: 'phone', type: 'text' },
    { name: 'type', type: 'select',
      options: ['partner', 'sponsor', 'intern', 'stipendiant'],
      required: true
    },
    { name: 'section', type: 'select',
      options: ['commercial', 'ngo', 'both'],
      required: true
    },
    { name: 'message', type: 'textarea' },
    { name: 'status', type: 'select',
      options: ['new', 'reviewed', 'accepted', 'rejected'],
      defaultValue: 'new'
    },
    { name: 'salesforceFunnelId', type: 'text' },
  ],
  hooks: {
    afterChange: [
      syncPartnerAppToSalesforce,  // → отдельная воронка в CRM
      sendAutoResponse,            // Автоматический первый ответ
    ],
  }
}
```

#### ContactSubmissions (Обратная связь)

```typescript
{
  slug: 'contact-submissions',
  fields: [
    { name: 'name', type: 'text', required: true },
    { name: 'email', type: 'email', required: true },
    { name: 'phone', type: 'text' },
    { name: 'subject', type: 'select',
      options: ['general', 'services', 'partnership', 'ngo', 'other'] },
    { name: 'message', type: 'textarea', required: true },
    { name: 'source', type: 'select',
      options: ['website', 'whatsapp', 'telegram'] },
    { name: 'status', type: 'select',
      options: ['new', 'bot_handled', 'assigned', 'resolved'],
      defaultValue: 'new'
    },
    { name: 'assignedTo', type: 'relationship', relationTo: 'users' },
    // Honeypot field (скрытое, для защиты от ботов)
    { name: 'website_url', type: 'text',
      admin: { description: 'Honeypot — should be empty' } },
  ],
  hooks: {
    beforeChange: [validateHoneypot, rateLimitCheck],
    afterChange: [syncToSalesforce, triggerAutoBot],
  }
}
```

#### Donations (Донаты)

```typescript
{
  slug: 'donations',
  fields: [
    { name: 'donor', type: 'relationship', relationTo: 'users' },
    { name: 'donorName', type: 'text' },  // Для анонимных
    { name: 'event', type: 'relationship', relationTo: 'events' },
    { name: 'amount', type: 'number', required: true },
    { name: 'currency', type: 'select', options: ['GEL', 'USD', 'EUR'] },
    { name: 'paymentMethod', type: 'select',
      options: ['bank_of_georgia', 'crypto'] },
    { name: 'status', type: 'select',
      options: ['pending', 'completed', 'failed', 'refunded'] },
    { name: 'isAnonymous', type: 'checkbox', defaultValue: false },
    { name: 'message', type: 'textarea' },
    { name: 'transactionId', type: 'text' },
  ]
}
```

### API Routes (основные)

```
POST   /api/payments/create      → Инициировать платёж (BOG / Crypto)
POST   /api/payments/bog         → Webhook от Bank of Georgia
POST   /api/payments/crypto      → Webhook от крипто-эквайринга

POST   /api/contact              → Отправка формы обратной связи
POST   /api/newsletter           → Подписка на рассылку
POST   /api/partnership/apply    → Заявка на партнёрство

POST   /api/ai/chat              → Чат с AI-ассистентом (streaming)
POST   /api/ai/train             → Обновить базу знаний AI

POST   /api/crm/sync             → Ручная синхронизация с Salesforce
POST   /api/crm/webhook          → Webhook от Salesforce

POST   /api/whatsapp/webhook     → Входящие сообщения WhatsApp
POST   /api/whatsapp/send        → Отправка сообщений

GET    /api/services              → Список услуг (публичный)
GET    /api/services/[slug]       → Детали услуги

GET    /api/dashboard/orders      → Заказы текущего пользователя
GET    /api/dashboard/invoices    → Счета текущего пользователя
GET    /api/dashboard/documents   → Документы текущего пользователя
POST   /api/dashboard/documents   → Загрузка документа
```

---

## 6. База данных

### Схема (PostgreSQL)

```
┌─────────────────┐     ┌──────────────────┐     ┌─────────────────┐
│     users       │     │    services      │     │     orders      │
├─────────────────┤     ├──────────────────┤     ├─────────────────┤
│ id              │     │ id               │     │ id              │
│ email           │     │ title (i18n)     │     │ orderNumber     │
│ password (hash) │     │ slug             │     │ client_id → users│
│ firstName (i18n)│     │ description(i18n)│     │ service_id      │
│ lastName (i18n) │     │ category         │     │ status          │
│ phone           │     │ pricing_type     │     │ amount          │
│ company         │     │ pricing_amount   │     │ currency        │
│ role            │     │ pricing_currency │     │ paymentStatus   │
│ preferredLang   │     │ published        │     │ paymentMethod   │
│ salesforceId    │     │ order            │     │ salesforceId    │
│ section         │     │ image_id         │     │ created_at      │
│ created_at      │     │ created_at       │     │ updated_at      │
│ updated_at      │     │ updated_at       │     └─────────────────┘
└─────────────────┘     └──────────────────┘              │
        │                                                  │
        │              ┌──────────────────┐               │
        │              │    invoices      │               │
        │              ├──────────────────┤               │
        │              │ id               │               │
        │              │ invoiceNumber    │               │
        ├──────────────│ client_id → users│               │
        │              │ order_id → orders│───────────────┘
        │              │ amount           │
        │              │ currency         │
        │              │ status           │
        │              │ dueDate          │
        │              │ paidDate         │
        │              │ pdf_id           │
        │              └──────────────────┘
        │
        │              ┌──────────────────┐
        │              │    documents     │
        │              ├──────────────────┤
        ├──────────────│ owner_id → users │
        │              │ order_id → orders│
        │              │ title            │
        │              │ file_id          │
        │              │ type             │
        │              │ confidential     │
        │              │ encryption_key   │ ← зашифрован
        │              │ created_at       │
        │              └──────────────────┘
        │
        │              ┌──────────────────────┐
        │              │    blog_posts        │
        │              ├──────────────────────┤
        │              │ id                   │
        │              │ title (i18n)         │
        │              │ slug                 │
        │              │ content (i18n)       │
        │              │ excerpt (i18n)       │
        │              │ category             │
        │              │ section              │
        │              │ author_id → users    │
        │              │ featuredImage_id     │
        │              │ status               │
        │              │ publishedDate        │
        │              │ viewCount            │
        │              └──────────────────────┘
        │
        │              ┌──────────────────────┐
        │              │    events            │
        │              ├──────────────────────┤
        │              │ id                   │
        │              │ title (i18n)         │
        │              │ slug                 │
        │              │ description (i18n)   │
        │              │ date, endDate        │
        │              │ location (i18n)      │
        │              │ isOnline             │
        │              │ meetingLink          │
        │              │ donationGoal         │
        │              │ donationRaised       │
        │              │ status               │
        │              └──────────────────────┘
        │
        │              ┌──────────────────────┐
        │              │    donations         │
        │              ├──────────────────────┤
        │              │ id                   │
        │              │ donor_id → users     │
        │              │ event_id → events    │
        │              │ amount, currency     │
        │              │ paymentMethod        │
        │              │ status               │
        │              │ isAnonymous          │
        │              │ transactionId        │
        │              └──────────────────────┘
        │
        │              ┌──────────────────────────┐
        │              │  partner_applications     │
        │              ├──────────────────────────┤
        │              │ id                        │
        │              │ applicantName             │
        │              │ company, email, phone     │
        │              │ type (partner/sponsor/    │
        │              │   intern/stipendiant)     │
        │              │ section (comm/ngo/both)   │
        │              │ message                   │
        │              │ status                    │
        │              │ salesforceFunnelId        │
        │              └──────────────────────────┘
        │
        │              ┌──────────────────────────┐
        │              │  contact_submissions      │
        │              ├──────────────────────────┤
        │              │ id                        │
        │              │ name, email, phone        │
        │              │ subject, message          │
        │              │ source (web/wa/tg)        │
        │              │ status                    │
        │              │ assignedTo → users        │
        │              │ honeypot                  │
        │              └──────────────────────────┘
        │
        │              ┌──────────────────────────┐
        │              │  newsletter_subscribers   │
        │              ├──────────────────────────┤
        │              │ id                        │
        │              │ email                     │
        │              │ name                      │
        │              │ preferredLang             │
        │              │ subscribedAt              │
        │              │ isActive                  │
        │              └──────────────────────────┘

*(БД и миграции в проекте не используются. Контент — Sanity.)*
```

### Redis — ключи и структура

```
Сессии:
  session:{userId}                → данные сессии

Rate Limiting:
  rate:ip:{ip}                    → счётчик запросов
  rate:contact:{ip}               → лимит формы обратной связи

Кэш:
  cache:services:list             → кэш списка услуг (TTL: 5 мин)
  cache:blog:recent               → последние статьи (TTL: 5 мин)
  cache:ai:context:{sessionId}    → контекст AI-чата (TTL: 30 мин)

Очереди (BullMQ):
  bull:email                      → очередь email-уведомлений
  bull:crm-sync                   → очередь синхронизации с Salesforce
  bull:ai-training                → очередь обучения AI
  bull:payment-check              → проверка статуса платежей
```

### S3/MinIO — структура хранилища

```
phoenix-finance-storage/
├── media/                  # Публичные медиа (изображения, видео)
│   ├── services/
│   ├── blog/
│   ├── events/
│   └── partners/
├── documents/              # ПРИВАТНЫЕ документы клиентов
│   ├── {userId}/
│   │   ├── {orderId}/
│   │   │   ├── contract.pdf.enc    # Зашифрованные файлы
│   │   │   └── report.pdf.enc
│   │   └── general/
│   └── ...
├── invoices/               # PDF счетов
│   └── {userId}/
│       └── INV-2025-001.pdf
└── backups/                # Бэкапы БД
```

---

## 7. CMS

### CMS: Sanity Studio → `/studio`

Контент редактируется в **Sanity Studio** (не Payload). Админка: `/studio`.

Справочно — возможная структура контента:

```
SANITY STUDIO (/studio)
├── Контент
│   ├── Услуги (Services)          → Создать/редактировать/удалить услугу
│   │   └── Поля: название, описание, цена, категория, изображение
│   │   └── Локализация: EN / GE / RU для каждого поля
│   │
│   ├── Статьи (Blog Posts)        → Создать/редактировать статью
│   │   └── Rich Text редактор с медиа-вставками
│   │   └── SEO: slug, meta description, OG image
│   │
│   ├── Видео (Videos)             → Добавить видео-контент
│   │   └── Ссылка на YouTube/Vimeo + описание
│   │
│   ├── События (Events)           → Управление событиями НКО
│   │   └── Дата, место, онлайн-ссылка, цель сбора
│   │
│   └── Медиа (Media)              → Библиотека файлов
│       └── Загрузка изображений, PDF, видео
│
├── Управление
│   ├── Заказы (Orders)            → Просмотр, изменение статуса
│   ├── Счета (Invoices)           → Генерация, отправка
│   ├── Документы (Documents)      → Просмотр загруженных файлов
│   ├── Заявки (Contact)           → Входящие обращения
│   ├── Партнёрские заявки         → Заявки на партнёрство
│   ├── Донаты (Donations)         → История донатов
│   └── Подписчики (Newsletter)    → Список подписчиков
│
├── Настройки (Globals)
│   ├── Настройки сайта            → Название, описание, логотип
│   ├── Реквизиты компании         → ИНН, адрес, телефон, банк
│   ├── Навигация                  → Редактирование меню
│   ├── Социальные сети            → Ссылки по разделам
│   │   ├── Коммерческая часть     → WhatsApp, Telegram, Instagram...
│   │   └── НКО                    → Свои соцсети
│   └── Рекламные баннеры          → Настройка рекламных мест
│
└── Пользователи (Users)
    └── Управление ролями: admin, manager, client, partner
```

### Роли и доступ

```
admin     → полный доступ ко всему
manager   → редактирование контента, просмотр заявок, управление заказами
client    → (личный кабинет в проекте не используется)
partner   → ограниченный просмотр (партнёрский раздел)
```

### Локализация в CMS

```
Каждое локализуемое поле (localized: true) получает вкладки в админке:
┌───────┬───────┬───────┐
│  EN   │  GE   │  RU   │
└───────┴───────┴───────┘
Маркетолог переключается между языками и заполняет контент.
В Sanity локализация настраивается в схемах (поля с массивом языков).
```

---

## 8. Платежи и эквайринг

### Архитектура платёжной системы

```
┌──────────────────────────────────────────────┐
│            Payment Provider Interface         │
│  (абстракция — легко добавить новые провайдеры) │
├──────────────────────────────────────────────┤
│                                              │
│  ┌────────────┐  ┌──────────┐  ┌─────────┐  │
│  │  Bank of   │  │  Crypto  │  │ Future   │  │
│  │  Georgia   │  │ Acquiring│  │ Provider │  │
│  │  (BOG API) │  │  (TBD)   │  │          │  │
│  └─────┬──────┘  └────┬─────┘  └────┬─────┘  │
│        │              │              │        │
│  ┌─────▼──────────────▼──────────────▼─────┐  │
│  │         PaymentProviderRegistry          │  │
│  │  register() │ getProvider() │ process()  │  │
│  └──────────────────────────────────────────┘  │
└──────────────────────────────────────────────┘
```

### Реализация

```typescript
// src/lib/integrations/payments/provider.ts

interface PaymentProvider {
  name: string;
  createPayment(data: CreatePaymentDTO): Promise<PaymentResult>;
  verifyWebhook(payload: unknown, signature: string): boolean;
  getPaymentStatus(transactionId: string): Promise<PaymentStatus>;
  refund(transactionId: string, amount?: number): Promise<RefundResult>;
}

class PaymentProviderRegistry {
  private providers: Map<string, PaymentProvider> = new Map();

  register(provider: PaymentProvider) {
    this.providers.set(provider.name, provider);
  }

  getProvider(name: string): PaymentProvider {
    const provider = this.providers.get(name);
    if (!provider) throw new Error(`Provider ${name} not registered`);
    return provider;
  }
}

// Регистрация провайдеров
const registry = new PaymentProviderRegistry();
registry.register(new BankOfGeorgiaProvider());
registry.register(new CryptoProvider());
// registry.register(new StripeProvider());  // Будущее
```

### Bank of Georgia — интеграция

```
Процесс оплаты:
1. Клиент выбирает услугу → нажимает "Оплатить"
2. Backend создаёт заказ (order) в БД
3. Backend вызывает BOG API → создаёт платёжную сессию
4. Клиент перенаправляется на страницу оплаты BOG
5. После оплаты BOG отправляет webhook → /api/payments/bog
6. Backend обновляет статус заказа
7. Backend синхронизирует с Salesforce
8. Клиент получает email-подтверждение

Поддерживаемые валюты: GEL, USD, EUR
Поддерживаемые методы: карта (Visa/MC), Apple Pay, Google Pay
```

### Крипто-эквайринг

```
Провайдеры (выбрать после получения готового сайта):
- Bitpay
- CoinGate
- NOWPayments
- Binance Pay

Архитектура аналогична BOG — реализуем через PaymentProvider interface.
Добавление нового провайдера = один файл + регистрация в registry.
```

### Поддерживаемые валюты и способы

```
┌──────────────┬──────────────┬──────────────┐
│   Провайдер  │   Валюты     │   Методы     │
├──────────────┼──────────────┼──────────────┤
│ Bank of      │ GEL, USD,    │ Visa, MC,    │
│ Georgia      │ EUR          │ Apple/Google │
├──────────────┼──────────────┼──────────────┤
│ Crypto       │ BTC, ETH,    │ Кошелёк,     │
│ (TBD)        │ USDT, USDC   │ QR-код       │
├──────────────┼──────────────┼──────────────┤
│ Будущие      │ расширяемо   │ расширяемо   │
│ провайдеры   │              │              │
└──────────────┴──────────────┴──────────────┘
```

---

## 9. AI-ассистент и боты

### AI-ассистент (RAG — Retrieval Augmented Generation)

```
┌─────────────────────────────────────────────────────────┐
│                    AI-АССИСТЕНТ                          │
│                                                         │
│  ┌──────────┐    ┌─────────────┐    ┌───────────────┐  │
│  │ Пользова-│    │  Knowledge  │    │   OpenAI API  │  │
│  │ тель     │───▶│  Base       │───▶│   (GPT-4o)    │  │
│  │ (вопрос) │    │  (RAG)      │    │               │  │
│  └──────────┘    └──────┬──────┘    └───────┬───────┘  │
│                         │                    │          │
│                ┌────────▼────────┐   ┌───────▼───────┐  │
│                │ Vector DB       │   │  Streaming    │  │
│                │ (pgvector /     │   │  Response     │  │
│                │  Pinecone)      │   │  → пользовате-│  │
│                │                 │   │  лю           │  │
│                │ Контент сайта:  │   └───────────────┘  │
│                │ - Услуги        │                       │
│                │ - FAQ           │                       │
│                │ - Статьи блога  │                       │
│                │ - Информация о  │                       │
│                │   компании      │                       │
│                └─────────────────┘                       │
└─────────────────────────────────────────────────────────┘
```

### Логика работы AI

```
1. ПРИВЕТСТВИЕ
   - Определяет язык пользователя (из локали сайта)
   - Представляется как ассистент Phoenix Finance
   - Предлагает помощь

2. НАВИГАЦИЯ ПО САЙТУ
   - "Где найти услуги по налоговому учёту?" → ссылка на услугу
   - "Как связаться с компанией?" → контакты

3. FAQ
   - Отвечает на частые вопросы из базы знаний
   - "Сколько стоит аудит?" → информация из каталога услуг

4. КОНТЕКСТНОЕ ПОНИМАНИЕ
   - Знает на какой странице находится пользователь
   - Адаптирует ответы под раздел (коммерч./НКО/обучение)

5. ПЕРЕДАЧА МЕНЕДЖЕРУ
   - Если AI не может помочь → предлагает связь с менеджером
   - Создаёт заявку в CRM с контекстом диалога

6. ОБУЧЕНИЕ / ДООБУЧЕНИЕ
   - Админ может добавлять вопросы-ответы через CMS
   - Автоматическое обновление базы знаний при изменении контента
   - Логирование неотвеченных вопросов для улучшения
```

### Реализация

```typescript
// src/lib/integrations/ai/assistant.ts

interface AIAssistantConfig {
  model: 'gpt-4o' | 'gpt-4o-mini';
  temperature: number;        // 0.3 — более "точный", менее креативный
  maxTokens: number;
  systemPrompt: string;       // Персональность ассистента
  knowledgeBase: string[];    // IDs документов в vector DB
}

// Системный промпт
const SYSTEM_PROMPT = `
Ты — AI-ассистент Phoenix Finance Revolution.
Ты помогаешь клиентам с вопросами о бухгалтерских услугах,
НКО-проектах и образовательных материалах.

Правила:
- Отвечай на языке пользователя (EN/GE/RU)
- Будь профессиональным, но дружелюбным
- Используй информацию только из базы знаний
- Если не знаешь ответ — предложи связаться с менеджером
- Не выдумывай информацию о ценах и услугах
- При сложных вопросах — предлагай записаться на консультацию
`;

// RAG Pipeline
async function handleQuery(query: string, locale: string, pageContext: string) {
  // 1. Генерируем embedding запроса
  const queryEmbedding = await generateEmbedding(query);

  // 2. Ищем релевантные документы в vector DB
  const relevantDocs = await vectorSearch(queryEmbedding, {
    locale,
    limit: 5,
  });

  // 3. Формируем контекст для GPT
  const context = relevantDocs.map(doc => doc.content).join('\n\n');

  // 4. Отправляем в OpenAI со streaming
  const stream = await openai.chat.completions.create({
    model: 'gpt-4o',
    stream: true,
    messages: [
      { role: 'system', content: SYSTEM_PROMPT },
      { role: 'system', content: `Контекст из базы знаний:\n${context}` },
      { role: 'system', content: `Пользователь на странице: ${pageContext}` },
      { role: 'user', content: query },
    ],
    temperature: 0.3,
  });

  return stream;
}
```

### Автоматизация первого касания (Бот)

```
Канал              Механизм                        Действие
──────────────────────────────────────────────────────────────────
Форма на сайте  →  AI-бот (первый ответ)        →  Классификация + ответ
WhatsApp        →  WhatsApp Business Bot          →  Приветствие + FAQ
Telegram        →  Telegram Bot (BotFather)       →  Приветствие + FAQ
Партнёрская     →  Автоответ по email + CRM       →  Шаблон + воронка
заявка
Заказ услуги    →  Email-подтверждение            →  Шаблон + статус
Донат           →  Email-благодарность            →  Шаблон
──────────────────────────────────────────────────────────────────

Переход к менеджеру:
- Если бот не может ответить (3 неудачных попытки)
- Если клиент запрашивает живого менеджера
- Если тема "сложная" (определяется AI)
- Уведомление менеджеру в Salesforce + email/push
```

---

## 10. Интеграции

### Salesforce CRM

```
┌─────────────────────────────────────────────────────────┐
│                  SALESFORCE INTEGRATION                   │
│                                                         │
│  ОБЪЕКТЫ, которые синхронизируем:                       │
│                                                         │
│  Phoenix → Salesforce:                                  │
│  ┌──────────────────┐    ┌──────────────────────┐      │
│  │ Users (clients)   │───▶│ Contacts              │      │
│  │ Orders            │───▶│ Opportunities          │      │
│  │ ContactSubmissions│───▶│ Leads                  │      │
│  │ PartnerApps       │───▶│ Leads (отд. воронка)  │      │
│  │ Donations         │───▶│ Custom: Donations      │      │
│  └──────────────────┘    └──────────────────────┘      │
│                                                         │
│  ВОРОНКИ в Salesforce:                                  │
│  1. Клиентская воронка (заказы бухуслуг)               │
│  2. Партнёрская воронка - коммерч. (скрытая)           │
│  3. Партнёрская воронка - НКО                          │
│  4. Воронка спонсоров                                   │
│  5. Воронка стажёров                                    │
│  6. Воронка стипендиатов                                │
│                                                         │
│  Синхронизация:                                         │
│  - Real-time (через webhooks/API при необходимости)    │
│  - Batch (ежедневная сверка через BullMQ job)          │
│  - Webhook (Salesforce → Phoenix через Outbound Messages)│
└─────────────────────────────────────────────────────────┘
```

### WhatsApp Business API

```
Интеграция через Meta WhatsApp Business Platform:

1. Webhook → /api/whatsapp/webhook
   - Получаем входящие сообщения
   - Обрабатываем через AI-ассистент
   - Если AI не справляется → переключаем на менеджера

2. Отправка сообщений → /api/whatsapp/send
   - Шаблонные сообщения (подтверждение заказа, статус)
   - Нешаблонные (ответы в рамках 24ч окна)

3. Quick Replies / Interactive Messages
   - Кнопки для быстрого ответа
   - Списки услуг
   - Ссылки на сайт

Альтернатива для MVP: Twilio WhatsApp API (проще интеграция)
```

### Социальные сети

```
КОММЕРЧЕСКАЯ ЧАСТЬ:              НКО:
┌──────────────────┐              ┌──────────────────┐
│ WhatsApp (чат)   │              │ WhatsApp (чат)   │
│ Telegram (канал) │              │ Telegram (канал) │
│ Instagram        │              │ Instagram        │
│ Facebook         │              │ Facebook         │
│ LinkedIn         │              │ LinkedIn         │
└──────────────────┘              │ YouTube          │
                                  └──────────────────┘

Реализация в CMS:
- Global "SocialLinks" с разделением по section (commercial / ngo)
- Маркетолог может менять ссылки через Sanity Studio (/studio)
- WhatsApp и Telegram — открывают диалог (deep links)
- Остальные — переход на профиль
```

### Email (Resend / SendGrid)

```
Транзакционные письма:
1. Регистрация — подтверждение email
2. Заказ — подтверждение, изменение статуса
3. Счёт — отправка PDF
4. Партнёрская заявка — автоответ
5. Форма обратной связи — подтверждение получения
6. Восстановление пароля

Рассылки (Newsletter):
- Через Resend / SendGrid Campaigns
- Подписка на новости через форму на сайте
- Сегментация по языку (EN/GE/RU)
```

---

## 11. Безопасность

### Форма обратной связи (без CAPTCHA)

```
Многоуровневая защита:

1. HONEYPOT FIELD
   - Скрытое поле (display: none через CSS, а не hidden)
   - Если заполнено → бот, отклоняем

2. RATE LIMITING (Redis)
   - 3 отправки с одного IP за 10 минут
   - 10 отправок с одного IP за час
   - Блокировка на 30 минут при превышении

3. TIME-BASED VALIDATION
   - Сохраняем timestamp при загрузке формы
   - Если отправка < 3 сек → бот, отклоняем

4. INPUT SANITIZATION
   - DOMPurify для очистки HTML
   - Zod валидация на сервере
   - Экранирование SQL (через ORM)

5. CSRF PROTECTION
   - Next.js встроенная защита от CSRF
   - Токены в cookies (SameSite: Strict)
```

### Безопасность документов

```
1. ШИФРОВАНИЕ ФАЙЛОВ
   - AES-256-GCM для каждого файла
   - Уникальный ключ на каждый документ
   - Ключи хранятся в отдельной таблице (не с файлом)

2. КОНТРОЛЬ ДОСТУПА
   - Документ доступен только owner и admin
   - Signed URLs с TTL (5 минут) для скачивания
   - Логирование всех операций с документами

3. ХРАНЕНИЕ
   - Файлы в S3/MinIO (не в публичной директории)
   - Отдельный bucket для приватных документов
   - Server-side encryption (SSE-S3)

4. ПЕРЕДАЧА
   - Только HTTPS
   - Content-Disposition: attachment (принудительное скачивание)
   - X-Content-Type-Options: nosniff
```

### Общая безопасность

```
HEADERS (через middleware):
- Strict-Transport-Security: max-age=31536000
- X-Frame-Options: DENY
- X-Content-Type-Options: nosniff
- Content-Security-Policy: строгая политика
- Referrer-Policy: strict-origin-when-cross-origin

АУТЕНТИФИКАЦИЯ:
- (Авторизация/сессии при необходимости — в проекте не используются)
- Хэширование паролей: bcrypt (cost factor 12)
- Rate limiting на login (5 попыток → блокировка 10 мин)
- CORS: whitelist только наши домены

ДАННЫЕ:
- Все пользовательские данные → валидация через Zod
- SQL-инъекции: БД в проекте не используется (контент в Sanity)
- XSS: React + DOMPurify + CSP headers
- Бэкапы БД: ежедневно, зашифрованные, в отдельном хранилище

СООТВЕТСТВИЕ:
- GDPR: consent management, право на удаление
- Cookie banner с выбором категорий
- Privacy Policy, Terms of Service
```

---

## 12. Развёртывание

*(Docker в проекте не используется. Деплой — Vercel или иной хостинг для Next.js.)*

### CI/CD (GitHub Actions)

```yaml
# .github/workflows/deploy.yml
name: Deploy
on:
  push:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: 20 }
      - run: npm ci
      - run: npm run lint
      - run: npm run type-check
      - run: npm run test

  deploy:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Deploy to production
        # Vercel (рекомендуется для Next.js + Sanity)
```

### Инфраструктура

```
Вариант A: Vercel (рекомендуется для старта)
  ├── App: Vercel (Next.js native)
  ├── DB: Neon (managed PostgreSQL)
  ├── Redis: Upstash (managed Redis)
  ├── Storage: Vercel Blob / AWS S3
  └── Worker: Vercel Cron + Edge Functions

Вариант B: VPS (Hetzner / DigitalOcean)
  ├── Nginx reverse proxy + SSL (Let's Encrypt)
  └── Node.js (Docker в проекте не используется)

Вариант C: Гибрид
  ├── App: Vercel
  ├── DB: Supabase (PostgreSQL + Auth)
  ├── Redis: Upstash
  ├── Storage: AWS S3
  └── Worker: Railway / Render
```

---

## 13. Итерации разработки

### ИТЕРАЦИЯ 1 — MVP Коммерческой части

```
Цель: Рабочий сайт с услугами и оплатой
(необходим для провайдеров платёжных услуг)

✅ Инфраструктура:
  - Инициализация проекта (Next.js + Sanity)
  - Настройка Tailwind + дизайн-система (цвета, типографика, компоненты)
  - Настройка i18n (EN/GE/RU) с определением по GeoIP

✅ Frontend — Лендинг:
  - Navbar (glassmorphism) с навигацией и языковым переключателем
  - Hero секция с Phoenix-анимацией
  - Обзор услуг (карточки с claymorphism)
  - Блок "Почему мы" со статистикой
  - Реквизиты компании (адрес, ИНН, телефон, банк)
  - Баннер подписки на новости (крупная кнопка)
  - Форма обратной связи (honeypot + rate limit)
  - Footer с соцсетями и навигацией
  - Соцсети коммерческой части (WhatsApp → диалог, Telegram → диалог)

✅ Frontend — Услуги:
  - Страница со списком всех услуг (/services)
  - Детальная страница услуги (/services/[slug])
  - Кнопка "Заказать" / "Получить консультацию"

✅ Backend:
  - Sanity: схемы контента (см. src/sanity/schemaTypes/)
  - API: /api/contact, /api/newsletter

✅ CMS:
  - Контент редактируется в Sanity Studio (/studio)
  - Локализация контента (EN/GE/RU)

✅ Безопасность:
  - Honeypot + rate limiting для форм
  - Security headers (HSTS, CSP, etc.)
  - HTTPS

✅ Деплой:
  - Production деплой (Vercel или VPS)
  - Домен + SSL
  - SEO: sitemap, robots.txt, meta tags

Начальный перечень бухгалтерских услуг:
  1. Ведение бухгалтерского учёта
  2. Налоговый учёт и декларирование
  3. Зарплатный учёт и расчёт
  4. Финансовая отчётность
  5. Регистрация бизнеса в Грузии
  6. Налоговое консультирование
  7. Аудит и ревизия
  8. Международный налоговый учёт
  9. Восстановление бухгалтерского учёта
  10. Ликвидация и реорганизация
  11. Консультация по грузинскому налоговому законодательству
  12. Подготовка бизнес-планов и финансовых моделей
```

### ИТЕРАЦИЯ 2 — Платежи + Личный кабинет

```
Цель: Клиенты могут оплачивать услуги и видеть свои заказы

✅ Платежи:
  - Интеграция Bank of Georgia API
  - Создание заказа → оплата → webhook → подтверждение
  - PaymentProvider абстракция (для будущих провайдеров)
  - Email-уведомления при оплате

✅ Личный кабинет:
  - Регистрация / Вход (/login, /register)
  - Dashboard layout (sidebar + header)
  - Мои заказы (/dashboard/orders)
  - Мои счета (/dashboard/invoices)
  - Загрузка/скачивание документов (/dashboard/documents)
  - Настройки профиля (/dashboard/settings)

✅ Backend:
  - Коллекции: Orders, Invoices, Documents
  - Шифрование документов (AES-256-GCM)
  - Signed URLs для скачивания
  - S3/MinIO для хранения файлов

✅ CMS:
  - (Личный кабинет/заказы не используются)
  - Генерация и отправка счетов
```

### ИТЕРАЦИЯ 3 — Salesforce CRM + Автоматизация

```
Цель: Все заявки и заказы синхронизируются с CRM

✅ Salesforce:
  - OAuth2 подключение к Salesforce
  - Синхронизация контактов (Users → Contacts)
  - Синхронизация заказов (Orders → Opportunities)
  - Синхронизация заявок (ContactSubmissions → Leads)
  - Отдельные воронки для партнёров, спонсоров, стажёров

✅ Автоматизация первого касания:
  - Auto-responder для форм обратной связи
  - Email-шаблоны для разных типов заявок
  - Уведомление менеджеру при новой заявке

✅ Background Workers (BullMQ):
  - Очередь email-уведомлений
  - Очередь CRM-синхронизации
  - Проверка статусов платежей
```

### ИТЕРАЦИЯ 4 — Крипто-эквайринг

```
Цель: Добавить крипто-платежи

✅ Крипто:
  - Выбор и интеграция крипто-провайдера
  - Реализация CryptoProvider (через PaymentProvider interface)
  - Webhook обработка
  - Отображение в личном кабинете
```

### ИТЕРАЦИЯ 5 — НКО раздел

```
Цель: Полноценный раздел для некоммерческой организации

✅ Frontend:
  - Главная НКО (/ngo)
  - Страница событий (/ngo/events, /ngo/events/[slug])
  - Публичный список партнёров НКО (/ngo/partners)
  - Страница донатов (/ngo/donate)
  - Виджет донатов на событиях
  - Соцсети НКО (отдельные от коммерч.)

✅ Backend:
  - Коллекции: Events, Partners, Donations
  - Платежи для донатов (BOG + Crypto)
  - CRM: воронки для НКО-партнёров и спонсоров
```

### ИТЕРАЦИЯ 6 — Обучение / Блог

```
Цель: Образовательный раздел со статьями и видео

✅ Frontend:
  - Главная обучения (/education)
  - Блог со статьями (/education/blog, /education/blog/[slug])
  - Видео-контент (/education/videos, /education/videos/[slug])
  - "Related articles" секция (как на jara.ge)
  - Фильтрация по категориям и тегам
  - Поиск по статьям

✅ Backend:
  - Коллекции: BlogPosts, Videos
  - Rich Text и медиа в Sanity Studio
  - ISR (Incremental Static Regeneration) для быстрой загрузки

✅ Рекламный баннер:
  - Место для контекстной рекламы (образовательный уклон)
  - CMS-управление баннерами через Sanity Studio (/studio)
```

### ИТЕРАЦИЯ 7 — Партнёрская программа

```
Цель: Система подачи заявок на партнёрство

✅ Frontend:
  - Обзор партнёрской программы (/partnership)
  - Для спонсоров (/partnership/sponsors)
  - Для стажёров (/partnership/interns)
  - Для стипендиатов (/partnership/stipendiants)
  - Формы подачи заявок

✅ Backend:
  - Коллекция: PartnerApplications
  - Auto-responder при подаче заявки
  - CRM: отдельная воронка для каждого типа партнёрства

✅ Важно:
  - Коммерческие партнёры — НЕ публичные (скрыты от конкурентов)
  - НКО-партнёры — публичный список на /ngo/partners
```

### ИТЕРАЦИЯ 8 — AI-ассистент

```
Цель: Интеллектуальный чат-бот на сайте

✅ AI:
  - Интеграция OpenAI API (GPT-4o)
  - RAG: векторная база из контента сайта (pgvector)
  - Плавающий чат-виджет (правый нижний угол)
  - Streaming ответов
  - Контекстное понимание (страница, раздел)
  - Переключение на менеджера

✅ Обучение:
  - Автоматическая индексация контента из CMS
  - Ручное добавление FAQ через Sanity Studio (/studio)
  - Логирование неотвеченных вопросов
```

### ИТЕРАЦИЯ 9 — WhatsApp интеграция

```
Цель: Обработка заявок через WhatsApp

✅ WhatsApp:
  - WhatsApp Business API (или Twilio)
  - Webhook для входящих сообщений
  - AI-бот для первого ответа
  - Переключение на менеджера
  - Шаблонные сообщения (подтверждения, статусы)
  - Ссылка "Написать в WhatsApp" на сайте → открытие диалога
```

### ИТЕРАЦИЯ 10 — Финальная полировка

```
✅ Оптимизация:
  - Performance audit (Lighthouse 90+)
  - SEO audit (все страницы, все языки)
  - Accessibility audit (WCAG AA)
  - Load testing

✅ Аналитика:
  - PostHog / Google Analytics 4
  - Event tracking (конверсии, формы, платежи)

✅ Документация:
  - Руководство для маркетолога (CMS)
  - API документация
  - Руководство по деплою
```

---

## Приложение A: Переменные окружения

```env
# .env.example

# --- App ---
NEXT_PUBLIC_SITE_URL=https://phoenix-finance.ge
NODE_ENV=production

# --- Database ---
DATABASE_URL=postgres://phoenix:password@localhost:5432/phoenix_finance
REDIS_URL=redis://localhost:6379

# --- Storage (S3/MinIO) ---
S3_ENDPOINT=https://s3.amazonaws.com
S3_ACCESS_KEY=...
S3_SECRET_KEY=...
S3_BUCKET=phoenix-finance-storage
S3_REGION=eu-central-1

# --- Payments ---
BOG_MERCHANT_ID=...
BOG_SECRET_KEY=...
BOG_API_URL=https://api.bog.ge/payments/v1
CRYPTO_PROVIDER_API_KEY=...

# --- Salesforce ---
SALESFORCE_CLIENT_ID=...
SALESFORCE_CLIENT_SECRET=...
SALESFORCE_REDIRECT_URI=...
SALESFORCE_INSTANCE_URL=https://yourinstance.salesforce.com

# --- OpenAI ---
OPENAI_API_KEY=sk-...
OPENAI_MODEL=gpt-4o

# --- WhatsApp ---
WHATSAPP_TOKEN=...
WHATSAPP_PHONE_NUMBER_ID=...
WHATSAPP_VERIFY_TOKEN=...

# --- Email ---
RESEND_API_KEY=re_...
EMAIL_FROM=hello@phoenix-finance.ge

# --- GeoIP ---
MAXMIND_LICENSE_KEY=...

# --- Encryption ---
DOCUMENT_ENCRYPTION_KEY=... (32 bytes, base64)
```

---

## Приложение B: Начальный список бухгалтерских услуг

| # | Услуга (RU) | Услуга (EN) | Категория |
|---|---|---|---|
| 1 | Ведение бухгалтерского учёта | Bookkeeping Services | accounting |
| 2 | Налоговый учёт и декларирование | Tax Accounting & Filing | tax |
| 3 | Зарплатный учёт | Payroll Management | payroll |
| 4 | Финансовая отчётность | Financial Reporting | reporting |
| 5 | Регистрация бизнеса в Грузии | Business Registration in Georgia | registration |
| 6 | Налоговое консультирование | Tax Consulting | consulting |
| 7 | Аудит и ревизия | Audit & Review | audit |
| 8 | Международный налоговый учёт | International Tax Accounting | international |
| 9 | Восстановление бух. учёта | Accounting Recovery | accounting |
| 10 | Ликвидация и реорганизация | Liquidation & Reorganization | consulting |
| 11 | Консультация по налоговому законодательству Грузии | Georgian Tax Law Consulting | consulting |
| 12 | Бизнес-план и финансовая модель | Business Plan & Financial Modeling | consulting |
| 13 | Учёт для ИП и малого бизнеса | Accounting for Sole Proprietors & SMEs | accounting |
| 14 | Учёт для НКО | Non-Profit Accounting | accounting |
| 15 | Подготовка к налоговой проверке | Tax Audit Preparation | tax |
